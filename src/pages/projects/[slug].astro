---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

export async function getStaticPaths() {
  const posts = await getCollection("projects");

  return posts.map((post) => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await post.render();

const monthYear = post.data.date.toLocaleDateString("en-GB", { month: "long", year: "numeric" });
---

<BaseLayout title={`${post.data.title} | Projects`}>
  <article class="mx-auto max-w-4xl px-6 py-12">
    <header class="mb-8">
      <a
        href="/projects"
        class="text-sm text-gray-500 dark:text-slate-400 hover:underline mb-4 inline-block"
      >
        ← Back to projects
      </a>

      {post.data.featuredImage && (
        <img
          src={post.data.featuredImage}
          alt={post.data.title}
          class="mb-6 w-full rounded-xl"
        />
      )}

      <h1 class="text-3xl font-bold mb-2">
        {post.data.title}
      </h1>

      <p class="text-gray-600 dark:text-slate-400 mb-4">
        <span class="italic">{monthYear}</span>
      </p>
    </header>

    <div class="flex flex-wrap gap-3 mb-8">
      {post.data.url_code && (
        <a
          href={post.data.url_code}
          target="_blank"
          class="inline-block rounded-md px-3 py-1.5 text-sm bg-slate-700 hover:bg-slate-600 text-white"
        >
          Code
        </a>
      )}

      {post.data.url_pdf && (
        <a
          href={post.data.url_pdf}
          target="_blank"
          class="inline-block rounded-md px-3 py-1.5 text-sm bg-slate-700 hover:bg-slate-600 text-white"
        >
          PDF
        </a>
      )}

      {post.data.url_slides && (
        <a
          href={post.data.url_slides}
          target="_blank"
          class="inline-block rounded-md px-3 py-1.5 text-sm bg-slate-700 hover:bg-slate-600 text-white"
        >
          Slides
        </a>
      )}

      {post.data.url_video && (
        <a
          href={post.data.url_video}
          target="_blank"
          class="inline-block rounded-md px-3 py-1.5 text-sm bg-slate-700 hover:bg-slate-600 text-white"
        >
          Video
        </a>
      )}
    </div>

    <div class="prose prose-neutral prose-tight max-w-none dark:prose-invert text-justify">
      <Content />
    </div>
  </article>
</ BaseLayout>
