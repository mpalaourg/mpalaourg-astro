---
import { getCollection } from 'astro:content';
import ProjectListing from '../../components/projects/ProjectListing.astro';

const posts = (await getCollection('projects')).sort((a, b) => (a.data.weight ?? 999) - (b.data.weight ?? 999));
---

<ProjectListing posts={posts} lang="en" />
