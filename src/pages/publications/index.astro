---
import { getCollection } from 'astro:content';
import PublicationListing from '../../components/publications/PublicationListing.astro';

const posts = (await getCollection('publications')).sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---

<PublicationListing posts={posts} lang="en" />
